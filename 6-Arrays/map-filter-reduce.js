/**
 * *
 * Map returns a NEW ARRAY containing the results of applying an operational on
 * all original array elements
 * current * 2
 *
 * Filter - returns a NEW ARRAY containing the array elements
 * that passed a speicified test condition
 * current > 2
 *
 * Reduce - boils(reduces) all array elements to a single value
 * i.e adding all elements together
 * acc + current
 * - keeps getting bigger and bigger (snowball effect)
 *
 */
